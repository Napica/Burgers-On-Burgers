<li>
    {{name}}
    <button class="change-devoured" data-id="{{this.id}}" data-newdevour="{{devoured}}">    
       {{#if devoured}} DEVOUR!! {{else}} Yum! {{/if}}
    </button>

    {{!-- <button class="delete-burger" data-id="{{this.id}}">Bad Burger!!</button> --}}
</li>