<li>
    {{name}}
    <button class="change-devoured" data-id="{{id}}" data-newdevour="{{devoured}}">    
       {{#if devoured}} DEVOUR!! {{else}} barf! {{/if}}
    </button>

    <button class="delete-burger" data-id="{{this.id}}">Bad Burger!!</button>
</li>